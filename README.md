# reviewHive

A multi-agent AI code reviewer for GitHub pull requests.

Three specialized agents examine a pull request diff in parallel — security and
correctness, style and maintainability, architecture — and their findings are
merged into a single review.

Work in progress.
